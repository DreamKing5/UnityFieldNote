// Safe to publish: use ONLY a Supabase publishable key (or legacy anon key).
// Never paste a secret key, service_role key, DB password or personal password here.
export const CONFIG = {
  supabaseUrl: "https://YOUR_PROJECT_REF.supabase.co",
  publishableKey: "YOUR_PUBLISHABLE_KEY"
};
